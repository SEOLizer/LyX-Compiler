[package]
name        = "std.time"
version     = "0.1.0"
author      = "Andreas Röne"
license     = "MIT"
description = "Zeit & Datum: time, datetime"

[sources]
files = ["time.lyx", "datetime.lyx"]

[dependencies]
std.alloc  = "^0.1.0"
std.string = "^0.1.0"
