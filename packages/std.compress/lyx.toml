[package]
name        = "std.compress"
version     = "0.1.0"
author      = "Andreas Röne"
license     = "MIT"
description = "Komprimierung: zlib/deflate"

[sources]
files = ["zlib.lyx"]

[dependencies]
std.alloc = "^0.1.0"
