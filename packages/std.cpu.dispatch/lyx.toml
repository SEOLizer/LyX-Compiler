[package]
name        = "std.cpu.dispatch"
version     = "0.1.0"
author      = "Andreas Röne"
license     = "MIT"
description = "Runtime SIMD dispatch: scalar / SSE2 / AVX2 selection for f32 array ops (WP-GPU-02)"

[sources]
files = ["cpu/dispatch.lyx"]

[dependencies]
std.alloc        = "^0.1.0"
std.cpu.features = "^0.1.0"
