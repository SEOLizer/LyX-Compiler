[package]
name        = "std.regex"
version     = "0.1.0"
author      = "Andreas Röne"
license     = "MIT"
description = "Reguläre Ausdrücke (NFA-basiert)"

[sources]
files = ["regex.lyx"]

[dependencies]
std.alloc  = "^0.1.0"
std.string = "^0.1.0"
