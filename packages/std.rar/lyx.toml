[package]
name        = "std.rar"
version     = "0.1.0"
author      = "Andreas Röne"
license     = "MIT"
description = "RAR5-Archiv Reader für stored-Einträge (method=0). Kein Writer — proprietäres Format."

[sources]
files = ["rar.lyx"]

[dependencies]
std.alloc  = "^0.1.0"
std.io     = "^0.1.0"
std.string = "^0.1.0"
std.fs     = "^0.1.0"
