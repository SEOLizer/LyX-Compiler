[package]
name        = "std.cpu.features"
version     = "0.1.0"
author      = "Andreas Röne"
license     = "MIT"
description = "CPU feature detection: SSE2, SSE4.1, AVX2 via /proc/cpuinfo (WP-GPU-02)"

[sources]
files = ["cpu/features.lyx"]

[dependencies]
std.alloc = "^0.1.0"
std.io    = "^0.1.0"
