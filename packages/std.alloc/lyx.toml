[package]
name        = "std.alloc"
version     = "0.1.0"
author      = "Andreas Röne"
license     = "MIT"
description = "Heap-Allokation (alloc/free) — Basis aller std-Pakete"

[sources]
files = ["alloc.lyx"]

