[package]
name        = "std.brotli"
version     = "0.1.0"
author      = "Andreas Röne"
license     = "MIT"
description = "Brotli-Kompressor (Store-Modus) und -Dekompressor (RFC 7932, ohne statisches Dict)"

[sources]
files = ["brotli.lyx"]

[dependencies]
std.alloc  = "^0.1.0"
std.io     = "^0.1.0"
std.string = "^0.1.0"
