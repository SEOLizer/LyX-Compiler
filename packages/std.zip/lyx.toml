[package]
name        = "std.zip"
version     = "0.1.0"
author      = "Andreas Röne"
license     = "MIT"
description = "ZIP-Archiv Lesen und Schreiben (method 0/stored + method 8/deflated)"

[sources]
files = ["zip.lyx"]

[dependencies]
std.alloc    = "^0.1.0"
std.io       = "^0.1.0"
std.string   = "^0.1.0"
std.fs       = "^0.1.0"
std.compress = "^0.1.0"
