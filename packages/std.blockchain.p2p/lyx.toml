[package]
name        = "std.blockchain.p2p"
version     = "0.1.0"
author      = "Andreas Röne"
license     = "MIT"
description = "Blockchain P2P-Netzwerk: TCP-Node, binary Protokoll, Deserializer (WP-BL-06)"

[sources]
files = ["blockchain/p2p.lyx"]

[dependencies]
std.alloc       = "^0.1.0"
std.blockchain  = "^0.1.0"
std.crypto.sha256 = "^0.1.0"
