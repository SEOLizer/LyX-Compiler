fn test1(v: int64, dst: int64): void {
  PrintStr("t1 start\n");
  poke64(dst + 0, v);
  PrintStr("t1 done\n");
}

fn main(): void {
  var buf: int64 := mmap(0, 16, 3, 34, -1, 0);
  PrintStr("buf=");
  PrintInt(buf);
  PrintStr("\n");
  test1(12345, buf);
}
