import std.alloc;
fn test1(v: int64, dst: int64): void {
  PrintLn("t1 start");
  poke64(dst + 0, v);
  PrintLn("t1 done");
}

fn main(): void {
  var buf: int64 := alloc(16);
  Print("buf=");
  PrintLn(buf);
  test1(12345, buf);
}
