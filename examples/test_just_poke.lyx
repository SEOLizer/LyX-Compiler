// Even simpler: just mmap and poke
fn main(): int64 {
    var addr: int64 := mmap(0, 16, 3, 34, -1, 0);
    Print("addr: ");
    PrintLn(addr);
    
    // Just poke
    poke8(addr, 65);
    PrintLn("poke done");
    
    return 0;
}