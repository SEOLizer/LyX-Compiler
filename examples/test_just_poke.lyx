// Even simpler: just mmap and poke
import std.alloc;
fn main(): int64 {
    var addr: int64 := alloc(16);
    Print("addr: ");
    PrintLn(addr);
    
    // Just poke
    poke8(addr, 65);
    PrintLn("poke done");
    
    return 0;
}