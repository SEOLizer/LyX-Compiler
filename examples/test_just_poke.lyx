// Even simpler: just mmap and poke
fn main(): int64 {
    var addr: int64 := mmap(0, 16, 3, 34, -1, 0);
    PrintStr("addr: ");
    PrintInt(addr);
    PrintStr("\n");
    
    // Just poke
    poke8(addr, 65);
    PrintStr("poke done\n");
    
    return 0;
}