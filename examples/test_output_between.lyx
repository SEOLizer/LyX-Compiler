// Test if output flushes something - do stdout between network ops
import std.net.syscalls;
import std.net.socket;
import std.alloc;

fn main(): int64 {
    var conn: TCPConn := TCPConnect(0x7F000001, 6379);
    if (conn.fd < 0) { return 1; }
    PrintLn("connected");
    
    var my_fd: int64 := conn.fd;
    var req: pchar := "*1\r\n$4\r\nPING\r\n";
    
    // First network op
    write(my_fd, req, 18);
    
    var mbuf: int64 := alloc(64);
    var n: int64 := read(my_fd, mbuf, 64);
    Print("read1 n = ");
    PrintLn(n);
    
    // DO stdout between operations
    PrintLn("--- output between network ops ---");
    
    // Second network op
    write(my_fd, req, 18);
    PrintLn("wrote2");
    
    n := read(my_fd, mbuf, 64);
    Print("read2 n = ");
    PrintLn(n);
    
    free(mbuf, 64);
    close(conn.fd);
    return 0;
}