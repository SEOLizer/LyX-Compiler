// Test DN parsing only

import std.net.ldap;
import std.io;

pub fn main(): int64 {
  var count: int64 := LDAPCountRDNs("cn=admin,dc=example,dc=com");
  PrintStr("Count: ");
  PrintIntLn(count);
  return 0;
}