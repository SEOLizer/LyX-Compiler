// Test LDAP filter only

import std.net.ldap;
import std.io;

pub fn main(): int64 {
  var filter: int64 := LDAPBuildFilterEqual("cn", "admin");
  if (filter != 0) {
    PrintStr("Filter created at: ");
    PrintIntLn(filter);
  }
  return 0;
}