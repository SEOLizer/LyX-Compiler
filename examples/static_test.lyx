// static_test.lyx - Statischer Test ohne externe Symbole

fn add(a: int64, b: int64): int64 {
  return a + b;
}

fn main(): int64 {
  var x: int64 := 10;
  var y: int64 := 32;
  var z: int64 := add(x, y);
  return z - 42;  // Sollte 0 returnen
}
