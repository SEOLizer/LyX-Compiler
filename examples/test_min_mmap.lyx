// Minimal mmap -> poke -> peek test
import std.alloc;
fn main(): int64 {
    var buf: int64 := alloc(64);
    poke8(buf, 65);
    var v: int64 := peek8(buf);
    PrintLn(v);
    free(buf, 64);
    return 0;
}