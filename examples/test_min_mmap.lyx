// Minimal mmap -> poke -> peek test
fn main(): int64 {
    var buf: int64 := mmap(0, 64, 3, 34, -1, 0);
    poke8(buf, 65);
    var v: int64 := peek8(buf);
    PrintInt(v);
    PrintStr("\n");
    munmap(buf, 64);
    return 0;
}