import std.alloc;
fn poke_arith(path: pchar, dst: int64): void {
  poke64(dst + 0, path as int64);
}

fn main(): void {
  var buf: int64 := alloc(16);
  Print("buf=");
  PrintLn(buf);
  poke_arith("/bin/echo", buf);
  PrintLn("done");
  free(buf, 16);
}
