var globalCounter: int64 := 0;

fn increment() {
  globalCounter := globalCounter + 1;
}

fn main(): int64 {
  PrintInt(globalCounter);
  PrintStr("\n");
  
  increment();
  increment();
  increment();
  
  PrintInt(globalCounter);
  PrintStr("\n");
  
  return 0;
}
