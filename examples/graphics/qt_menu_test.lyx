// Qt Menu Test — Tests WP8 Menu/Toolbar API
import std.qt5_app;

pub fn main(): int64 {
  PrintStr("=== Qt Menu Test (WP8) ===\n");
  
  // Initialize Qt
  var ok: int64 := qt_init();
  if (ok == 0) { PrintStr("Qt init failed\n"); return 1; }

  // Create main window
  var win: int64 := qt_create_window("Menu Test", 400, 300);
  
  // Test menu API stubs (WP8)
  PrintStr("\n--- Testing WP8 Menu API ---\n");
  
  // Try to create menu bar (returns 0 - stub)
  var menubar: int64 := 0; // qt_menubar_create(win);
  PrintStr("MenuBar handle: "); PrintInt(menubar); PrintStr("\n");
  
  // Try to create menu (returns 0 - stub)
  var file_menu: int64 := 0; // qt_menu_create(0);
  PrintStr("Menu handle: "); PrintInt(file_menu); PrintStr("\n");
  
  // Try to create action (returns 0 - stub)
  var exit_action: int64 := 0; // qt_action_create(0, 0);
  PrintStr("Action handle: "); PrintInt(exit_action); PrintStr("\n");
  
  // Try to create toolbar (returns 0 - stub)
  var toolbar: int64 := 0; // qt_toolbar_create(win, 0);
  PrintStr("Toolbar handle: "); PrintInt(toolbar); PrintStr("\n");
  
  // Test dialog stubs (WP9)
  PrintStr("\n--- Testing Dialog Stubs ---\n");
  
  // Message box stub
  // qt_msgbox_show(0, "Test message", 0);
  PrintStr("MessageBox tested (stub)\n");
  
  // File dialog stub
  // var filename: int64 := qt_file_open_dialog(0, "Open File", 0);
  PrintStr("FileOpen tested (stub)\n");
  
  // Input dialog stub
  // var input: int64 := qt_input_dialog(0, "Title", "Label", 0);
  PrintStr("InputDialog tested (stub)\n");
  
  // Show window
  qt_show(win);
  PrintStr("\nWindow shown — close it to exit\n");
  
  // Run event loop
  var code: int64 := qt_exec();
  
  PrintStr("Qt exited with code: ");
  PrintInt(code);
  PrintStr("\n=== TEST COMPLETE ===\n");
  
  return code;
}