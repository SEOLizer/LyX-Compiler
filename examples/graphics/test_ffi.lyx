// Test: strcmp with FFI
import std.io;

extern fn strcmp(s1: pchar, s2: pchar): int64 link "libc.so.6";

pub fn main(): int64 {
  var result: int64 := strcmp("hello", "hello");
  PrintStr("strcmp result: ");
  PrintInt(result);
  PrintStr("\n");
  return 0;
}