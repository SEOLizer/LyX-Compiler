// Minimal test of syscalls for Redis connection
import std.db.redis;

fn main(): int64 {
    PrintLn("=== Minimal Redis Test ===");
    
    // Just call RedisConnect - but it expects a string which might cause issues
    var conn: RedisConn := RedisConnect("127.0.0.1", 6379);
    
    Print("fd = ");
    PrintLn(conn.fd);
    
    return 0;
}