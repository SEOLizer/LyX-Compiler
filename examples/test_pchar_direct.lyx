fn poke_direct(path: pchar, dst: int64): void {
  poke64(dst, path as int64);
}

fn main(): void {
  var buf: int64 := mmap(0, 16, 3, 34, -1, 0);
  PrintStr("buf=");
  PrintInt(buf);
  PrintStr("\n");
  poke_direct("/bin/echo", buf);
  PrintStr("done\n");
  munmap(buf, 16);
}
