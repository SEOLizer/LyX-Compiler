// Fibonacci mit dynamischer Programmierung
// Demonstrates: Rekursion, iterative Berechnung

// Iterative Fibonacci (DP-Stil)
fn fib_iterative(n: int64): int64 {
    if (n <= 1) {
        return n;
    }
    
    var prev: int64 := 0;
    var curr: int64 := 1;
    var temp: int64 := 0;
    var i: int64 := 2;
    
    while (i <= n) {
        temp := curr;
        curr := curr + prev;
        prev := temp;
        i := i + 1;
    }
    
    return curr;
}

fn main(): int64 {
    var n: int64 := 10;
    
    Print("Fibonacci (iterativ) von ");
    PrintLn(n);
    Print(": ");
    PrintLn(fib_iterative(n));
    PrintLn("");
    
    n := 20;
    Print("Fibonacci (iterativ) von ");
    PrintLn(n);
    Print(": ");
    PrintLn(fib_iterative(n));
    PrintLn("");
    
    n := 30;
    Print("Fibonacci (iterativ) von ");
    PrintLn(n);
    Print(": ");
    PrintLn(fib_iterative(n));
    PrintLn("");
    
    n := 40;
    Print("Fibonacci (iterativ) von ");
    PrintLn(n);
    Print(": ");
    PrintLn(fib_iterative(n));
    PrintLn("");
    
    return 0;
}