// Test unit for param order

unit test.params;

pub fn test(a: int64, b: int64): int64 {
    PrintInt(a);  // Should Print 10
    PrintStr("\n");
    PrintInt(b);  // Should Print 5
    PrintStr("\n");
    return a;
}
