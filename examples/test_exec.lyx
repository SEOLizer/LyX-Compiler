// Test exec() - fork+execve+wait4 via mpg123
import std.os;

fn main(): void {
  PrintStr("Testing exec: echo hello\n");
  var ret: int64 := exec("/bin/echo", "hello from exec", "", "", "");
  PrintStr("exec returned: ");
  PrintInt(ret);
  PrintStr("\n");
}
