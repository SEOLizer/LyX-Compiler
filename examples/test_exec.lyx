// Test exec() - fork+execve+wait4 via mpg123
import std.os;

fn main(): void {
  PrintLn("Testing exec: echo hello");
  var ret: int64 := exec("/bin/echo", "hello from exec", "", "", "");
  Print("exec returned: ");
  PrintLn(ret);
}
