// Test: pchar local vs pchar param in poke64
import std.alloc;
fn poke_via_param(path: pchar, dst: int64): void {
  var val: int64 := path as int64;
  Print("param as int64: ");
  PrintLn(val);
  poke64(dst, val);
}

fn poke_via_local(dst: int64): void {
  var path: pchar := "/bin/echo";
  var val: int64 := path as int64;
  Print("local as int64: ");
  PrintLn(val);
  poke64(dst, val);
}

fn main(): void {
  var buf: int64 := alloc(16);
  Print("buf=");
  PrintLn(buf);

  PrintLn("Test local:");
  poke_via_local(buf);
  PrintLn("local ok");

  PrintLn("Test param:");
  poke_via_param("/bin/echo", buf);
  PrintLn("param ok");
  free(buf, 16);
}
