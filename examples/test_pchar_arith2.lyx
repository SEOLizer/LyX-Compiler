import std.alloc;
fn poke_arith(path: pchar, dst: int64): void {
  poke64(dst + 8, path as int64);
}

fn main(): void {
  var buf: int64 := alloc(32);
  poke_arith("/bin/echo", buf);
  var v: int64 := peek64(buf + 8);
  Print("v=");
  PrintLn(v);
  free(buf, 32);
}
