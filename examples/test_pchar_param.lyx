import std.os;
import std.alloc;

fn test_poke(path: pchar): void {
  var argv: int64 := alloc(16);
  Print("argv=");
  PrintLn(argv);
  poke64(argv + 0, path as int64);
  PrintLn("poke done");
  free(argv, 16);
}

fn main(): void {
  test_poke("/bin/echo");
}
