# Lyx Compiler — Milestone V1.1.0 (Solidify / Beta-Reife)

**Basis:** V1.0.0A (released, Tag `v1.0.0A`, Singularität bb03cce)

**Thema:** Härtung + Vollständigkeit statt neuer Breite. Ziel: V1.x produktionsnah
und vertrauenswürdig machen — Backend-Parität abschließen, FFI-Sandbox vollständig
fail-closed, restliche Security-Punkte, bekannte Bugs, sauberes Packaging.

**Konvention:** WP-ID, Status ⬜ Offen / 🔄 / ✅. Branch `feat/v11-<id>` von `develop`.

---

## Übersicht

| WP    | Titel                                                   | Prio | Status |
|-------|---------------------------------------------------------|------|--------|
| S1    | WP-A4: `@big_endian` ARM64 REV-Emission (Backend-Parität) | P0 | ⬜ |
| S2    | Kleine Korrektheits-Bugs (std-path off-by-one, Makefile VERSION) | P0 | ⬜ |
| S3    | FFI-Sandbox vollständig fail-closed (no-link + PROCESS-Klasse) | P0 | ⬜ |
| S4    | Security #28 Kernel-Mode-Guard-Erweiterung              | P1 | ⬜ |
| S5    | Security #29 Lizenz-Secret-Architektur (XOR → asymmetrisch) | P1 | ⬜ |
| S6    | `.deb`-Packaging @ 1.0.0A funktionsfähig + Install-Test | P1 | ⬜ |
| S7    | Doc-Sync: stale Status-Tabellen (security.md, milestones) | P2 | ⬜ |
| S8    | Test/CI-Abdeckungs-Audit (WP-37-Klasse: jeder Fix getestet) | P2 | ⬜ |
| S9    | WP-09 Windows-ARM64-VMT Hardware-Verifikation           | P3 | ⬜ (Hardware) |

---

## P0 — Muss für V1.1.0

### S1 — WP-A4: `@big_endian` ARM64 REV-Emission
**Stand (aus v1.0.0A):** Parser, Sema, x86_64-Codegen (BSWAP/XCHG) fertig. ARM64-Backend fehlt.
**Aufgabe:** `REV16`/`REV32`/`REV64` nach Load / vor Store im ARM64-Emitter (`src/backend/arm64/emit_arm64.lyx`).
**Abnahme:** Round-trip auf ARM64 `hdr.x := v; var y := hdr.x;` → `y==v`; REV32 im Binary sichtbar. Schließt die letzte offene P1 aus v1.0.0A → Backend-Parität komplett.

### S2 — Kleine Korrektheits-Bugs
- **`--std-path` off-by-one** (`src/lyxc.lyx:793` `arg + 10` → `arg + 11`): liefert sonst `=PATH`. Test: `--std-path=/x` setzt `/x` (nicht `=/x`).
- **Makefile `VERSION`** (`Makefile:16` `0.9.10D` → `1.0.0A`): `.deb`-Paketname/-Version stale.
**Abnahme:** beide gefixt; je 1 Regressionstest/Check.

### S3 — FFI-Sandbox vollständig fail-closed
V1.0 schloss den Fail-Open im **link-Branch** (#815, FFI_CLASS_UNKNOWN + ffiTrusted). Reste:
- **no-link-Branch** (`_checkFuncDecl`, c3==-1): unbekannte no-link-Externs in User-Code laufen noch durch (nur Blacklist). Auf dieselbe UNKNOWN-Fail-Closed-Logik bringen.
- **PROCESS-Klasse** (fork/kill/clone…): `_checkExternFn` behandelt FORBIDDEN/OS/UNKNOWN, aber **PROCESS fällt durch → ohne @cap erlaubt**. Sollte wie OS @capabilities/@cap erfordern.
**Abnahme:** `extern fn fork()` (no-link, user) ohne @cap → Fehler; std-Builtins/trusted unverändert; make test grün.

---

## P1 — Sollte in V1.1.0

### S4 — Security #28: Kernel-Mode-Guard-Erweiterung
`std.net.epoll` + gesamtes `std.net.*` in die `@kernel_mode`-Forbidden-Allowlist (POSIX-abhängige Imports in `--target=lyxos-kernel` blocken). Verifizieren gegen Code (Doku evtl. stale). Test ergänzen.

### S5 — Security #29: Lizenz-Secret-Architektur
XOR-Obfuskation → asymmetrisch (das Lizenz-Secret liegt aktuell verschleiert im Binary). Ed25519-Infra existiert bereits (WP-29). Test.

### S6 — `.deb`-Packaging
`make package` zu einem installierbaren `.deb @ 1.0.0A` bringen (sync-units + precompile-units + install-bin). Abnahme: `dpkg -i` installiert, `lyxc --version` = 1.0.0A, std-Units auffindbar.

---

## P2 / P3

### S7 — Doc-Sync
`work/security.md`-Übersichtstabellen + `v1.0.0A-milestones.md` auf Realität bringen (markieren ⬜ für längst Erledigtes — wiederholte Verwirrungsquelle diese Session). Optional: ein Skript, das Status gegen vorhandene `sec_*`-Tests prüft.

### S8 — Test/CI-Abdeckungs-Audit
Sicherstellen, dass JEDER Security-/Korrektheits-Fix einen CI-Test hat (Lehre WP-37: Doku-✅ ≠ Code). Lücken schließen.

### S9 — WP-09 Windows-ARM64-VMT
Hardware-Lauf (`tests/wp09_win_arm64_vmt.lyx` → 3/60/PASS). Blockiert auf echtem Gerät.

---

## Nicht in V1.1.0 (→ V1.2+)
- **LyxOS-Reloc-Consumer / Vega-UI** (Flagship, eigene Session — Kickoff `Knowledge/LyxOS/Reloc-Consumer-START-HERE.md`)
- **Stdlib-Breite** E1-E5 (Audio/ALSA, Server-TLS, MySQL, Redis, LyxVision-Widgets)
- work/-Features (gRPC, gcp-sdk, image-decoder, blockchain, hl7, graphdb, wasm, packagemanager…)

---

## Release-Definition V1.1.0
- Alle P0 (S1-S3) ✅
- ≥ 2 von 3 P1 (S4-S6) ✅
- `make test` grün, Singularität S3==S4
- CHANGELOG + Version-Bump (1.0.0A → 1.1.0), Seed re-snap, Tag `v1.1.0`
