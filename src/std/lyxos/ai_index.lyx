// src/std/lyxos/ai_index.lyx — LX-15 Embedding & Vektorindex-Konstanten

// sys_ai_index_create flags
// #2035: Interface-Konstanten, siehe lyra.lyx — das pub fehlte an allen.
pub con IDX_FLAT  : int64 := 0   // Exakte Brute-Force-Suche
pub con IDX_HNSW  : int64 := 1   // Approximative ANN-Suche (Hierarchical NSW)
