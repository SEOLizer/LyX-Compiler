// LX-15 Test: KI-Embedding & Vektorindex (0x0807–0x080C) — nur Kompilierung prüfen.

import src.std.lyxos.ai_index;

fn main(): int64 {
    // sys_ai_index_create / sys_ai_index_insert / sys_ai_index_delete
    var idx: int64 := sys_ai_index_create(128, IDX_HNSW);
    sys_ai_index_insert(idx, 1, 0 as pchar, 128, 0 as pchar, 0);
    sys_ai_index_delete(idx, 1);

    // sys_ai_token_count
    var ntok: int64 := sys_ai_token_count(0, "Hallo"c, 5);

    // sys_ai_embed (5 args)
    sys_ai_embed(0, "Hallo"c, 5, 0 as pchar, 0 as pchar);

    // sys_ai_search (6 args)
    var found: int64 := sys_ai_search(idx, 0 as pchar, 128, 5, 0 as pchar, 0);

    return found;
}
