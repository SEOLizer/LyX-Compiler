// WP-R11: network.tcp.connect → Proxy-Prozess wird gestartet (fork+socketpair).
// Proxy installiert eigenen seccomp (nur Netzwerk). Elter beendet sich sauber.
// Proxy endet via EOF auf child_fd wenn Elter beendet wird.

@capabilities([network.tcp.connect])
fn main(): int64 {
  return 0;
}
