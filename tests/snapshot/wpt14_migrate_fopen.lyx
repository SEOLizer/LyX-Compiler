// WP-T14: --migrate-capabilities erkennt fopen-Aufruf.
// Kriterium 2: Output enthält fs.read(path: "/etc/x")

extern fn fopen(path: pchar, mode: pchar): int64 link "libc.so.6";
extern fn fclose(f: int64): int64 link "libc.so.6";

fn main(): int64 {
  var f: int64 := fopen("/etc/x"c, "r"c);
  fclose(f);
  return 0;
}
