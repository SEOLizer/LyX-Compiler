// WP-T14: --migrate-capabilities erkennt extern fn Capabilities.
// Dieses Programm selbst hat kein @capabilities — das ist der Migrationsfall.
// Das Binary gibt exit 0 zurück (stdout ist leer da kein PrintLn).

fn main(): int64 {
  return 0;
}
