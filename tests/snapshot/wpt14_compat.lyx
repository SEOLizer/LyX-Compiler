// WP-T14: --capabilities=compat — LCBS-Programm ohne seccomp/landlock kompilieren.
// Mit normalem lyxc würde seccomp+landlock aktiv sein.
// Mit --capabilities=compat ist kein seccomp/landlock aktiv → kein SIGSYS bei exit.

@capabilities([fs.read])
fn main(): int64 {
  return 0;
}
