// WP-R10 AC3: hardware.gpio → Landlock öffnet /dev und erstellt Regel
// für READ_FILE|WRITE_FILE|EXECUTE|READ_DIR auf /dev.
// Kein Crash, exit 0.

@capabilities([hardware.gpio])
fn main(): int64 {
  return 0;
}
