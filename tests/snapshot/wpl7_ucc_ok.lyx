// WP-L7 AC1: @uses_caller_cap — Aufruf aus Funktion MIT der benötigten Capability → OK

@uses_caller_cap([fs.write])
fn logToFile(): void {
  // Nutzt fs.write vom Aufrufer (hier: Simulation)
}

@capabilities([fs.write(path: "/var/log")])
fn writer(): void {
  logToFile();  // OK: writer hat fs.write
}

fn main(): int64 {
  writer();
  PrintLn("ok");
  return 0;
}
