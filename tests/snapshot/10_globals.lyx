var counter: int64;

fn increment() {
  counter := counter + 1;
}

fn main(): int64 {
  counter := 0;
  increment();
  increment();
  increment();
  PrintInt(counter);
  PrintStr("\n");
  return 0;
}
