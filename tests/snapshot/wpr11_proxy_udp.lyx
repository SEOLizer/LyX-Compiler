// WP-R11: network.udp.connect → UDP-Proxy gestartet.
// Proxy-BPF erlaubt nur socket/connect/sendto/recvfrom + read/write/close.
// Kein Dateizugriff im Proxy möglich (seccomp blockiert openat).

@capabilities([network.udp.connect])
fn main(): int64 {
  return 0;
}
