pub enum Color {
  RED,
  GREEN,
  BLUE,
};

pub enum HttpStatus {
  OK    = 200,
  NOT_FOUND = 404,
};

fn main(): int64 {
  PrintInt(RED);    Print("\n");
  PrintInt(GREEN);  Print("\n");
  PrintInt(BLUE);   Print("\n");
  PrintInt(OK);     Print("\n");
  PrintInt(NOT_FOUND); Print("\n");

  var c: Color := GREEN;
  if (c == GREEN) {
    Print("green\n");
  }
  return 0;
}
