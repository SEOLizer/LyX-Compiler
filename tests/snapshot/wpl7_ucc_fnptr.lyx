// WP-L7 AC4: @uses_caller_cap — Funktionszeiger → Compiler-Warnung (nicht überprüfbar)

@uses_caller_cap([fs.write])
fn logToFile(): void {
}

fn main(): int64 {
  var fp: fn(): void := logToFile;  // Warnung: @uses_caller_cap bei indirektem Aufruf
  PrintLn("fnptr ok");
  return 0;
}
