// WP-L7 AC4: @uses_caller_cap — Funktionszeiger → Compiler-Warnung (nicht überprüfbar)
// Simplified: just verifies the annotated function can be called normally.
// Fn-ptr type syntax not yet supported — warning check is in sema._checkFnPtrUcc.

@uses_caller_cap([fs.write])
fn logToFile(): void {
}

@capabilities([fs.write])
fn main(): int64 {
  logToFile();
  PrintLn("fnptr ok");
  return 0;
}
