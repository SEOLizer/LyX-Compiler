// WP-T13: Audit-Output für LCBS-Programm mit fs.read + hardware.gpio.
// Score = 40/40: alle Kriterien erfüllt (ohne PIE/Stack Canaries).
// Audit-Output geht auf stderr, stdout ist leer.

@capabilities([fs.read, hardware.gpio(pin: 4)])
fn main(): int64 {
  return 0;
}
