// WP-L7 AC2: @uses_caller_cap — Aufruf aus Funktion OHNE benötigte Capability → Fehler

@uses_caller_cap([fs.write])
fn logToFile(): void {
}

fn noCapCaller(): void {
  logToFile();
}

fn main(): int64 {
  noCapCaller();
  return 0;
}
