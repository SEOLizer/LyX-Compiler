// 18_random.lyx — Random() und RandomSeed() Builtins
fn main(): int64 {
  RandomSeed(42);

  var r1: int64 := Random();
  var r2: int64 := Random();
  var r3: int64 := Random();

  if (r1 == 0) { Print("FAIL: r1 ist 0\n"); return 1; }
  if (r2 == r1) { Print("FAIL: r2 == r1\n"); return 1; }
  if (r3 == r2) { Print("FAIL: r3 == r2\n"); return 1; }

  // Reseed → gleiche Sequenz
  RandomSeed(42);
  var r1b: int64 := Random();
  if (r1b != r1) { Print("FAIL: reseed liefert andere Sequenz\n"); return 1; }

  Print("OK\n");
  return 0;
}
