// WP-H12: hardware.gpio(pin: 18) → Landlock-Regel für /dev/gpiochip0.
// Pins 0-31 mappen auf gpiochip0, Pins 32-53 auf gpiochip1.
// Wenn /dev/gpiochip0 nicht vorhanden: openat schlägt fehl, Regel wird übersprungen.

@capabilities([hardware.gpio(pin: 18)])
fn main(): int64 {
  return 0;
}
