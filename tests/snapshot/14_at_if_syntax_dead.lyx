// WP-CS-11: Parse-Fehler im toten @if-Zweig wird trotzdem gemeldet
// Der Parser parst beide Zweige — nur der aktive Zweig geht durch Sema.
// @@@  ist kein gültiges Lyx-Token → Parse-Fehler, auch wenn Zweig tot ist.

@if (false) {
  fn broken(): int64 { return @@@; }
}

fn main(): int64 { return 0; }
