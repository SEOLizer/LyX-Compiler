// WP-L8 AC-Fehler: Klasse deklariert @capabilities([]) (leer), aber Methode
// ruft @uses_caller_cap([fs.write])-Fn auf → fs.write fehlt → Compiler-Fehler

@uses_caller_cap([fs.write])
fn writeLog(): void {
}

@capabilities([])
pub type Logger = class {
  fn log(): void {
    writeLog();  // Fehler: Logger hat keine fs.write-Capability
  }
};

fn main(): int64 {
  return 0;
}
