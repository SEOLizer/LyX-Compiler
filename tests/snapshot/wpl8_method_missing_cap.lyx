// WP-L8 AC-Fehler: Methode ohne Klassen-Capability ruft @uses_caller_cap-Fn auf → Fehler

@uses_caller_cap([fs.write])
fn writeLog(): void {
}

pub type Logger = class {
  fn log(): void {
    writeLog();  // Fehler: Logger hat keine fs.write-Capability
  }
};

fn main(): int64 {
  return 0;
}
