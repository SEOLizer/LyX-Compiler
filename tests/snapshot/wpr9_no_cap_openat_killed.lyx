// WP-R9 AC1: Programm ohne Capabilities → open() → SIGSYS
// open() (SYS_open=2) nicht in impliziten Caps → seccomp tötet den Prozess

@capabilities([])
fn main(): int64 {
  var fd: int64 := open("/dev/null"c, 0, 0);
  return fd;
}
