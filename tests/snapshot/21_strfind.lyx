// 21_strfind.lyx — StrFind Builtin (Substring-Suche)
fn main(): int64 {
  if (StrFind("hello world", "world") != 6)  { Print("FAIL: basic\n");         return 1; }
  if (StrFind("hello world", "hello") != 0)  { Print("FAIL: at start\n");      return 1; }
  if (StrFind("hello world", "xyz")   != -1) { Print("FAIL: not found\n");     return 1; }
  if (StrFind("hello world", "")      != 0)  { Print("FAIL: empty needle\n");  return 1; }
  if (StrFind("", "abc")              != -1) { Print("FAIL: empty hs\n");      return 1; }
  if (StrFind("aaab", "aab")          != 1)  { Print("FAIL: overlap\n");       return 1; }
  if (StrFind("ab", "abcd")           != -1) { Print("FAIL: needle longer\n"); return 1; }
  Print("OK\n");
  return 0;
}
