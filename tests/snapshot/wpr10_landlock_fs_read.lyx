// WP-R10 AC1/AC4: fs.read → Landlock installiert READ_FILE|READ_DIR auf "/"
// seccomp erlaubt openat(O_RDONLY), Landlock erlaubt Lesen aller Pfade.
// Kein Crash beim Installieren von Landlock → exit 0.

@capabilities([fs.read])
fn main(): int64 {
  return 0;
}
