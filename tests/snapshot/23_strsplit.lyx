// 23_strsplit.lyx — StrSplit Builtin
fn main(): int64 {
  // Basic split: "a,b,c" by ","
  var arr1: pchar[] := StrSplit("a,b,c", ",");
  if (!StrEquals(arr1[0], "a")) {
    Print("FAIL: arr1[0]="); Print(arr1[0]); Print("\n"); return 1;
  }
  if (!StrEquals(arr1[1], "b")) {
    Print("FAIL: arr1[1]="); Print(arr1[1]); Print("\n"); return 1;
  }
  if (!StrEquals(arr1[2], "c")) {
    Print("FAIL: arr1[2]="); Print(arr1[2]); Print("\n"); return 1;
  }

  // No delimiter found → single element
  var arr2: pchar[] := StrSplit("hello", ",");
  if (!StrEquals(arr2[0], "hello")) {
    Print("FAIL: arr2[0]="); Print(arr2[0]); Print("\n"); return 1;
  }

  // Leading delimiter → first element is empty string
  var arr3: pchar[] := StrSplit(",a", ",");
  if (!StrEquals(arr3[0], "")) {
    Print("FAIL: arr3[0] not empty\n"); return 1;
  }
  if (!StrEquals(arr3[1], "a")) {
    Print("FAIL: arr3[1]="); Print(arr3[1]); Print("\n"); return 1;
  }

  // Multi-char delimiter
  var arr4: pchar[] := StrSplit("x::y::z", "::");
  if (!StrEquals(arr4[0], "x")) {
    Print("FAIL: arr4[0]="); Print(arr4[0]); Print("\n"); return 1;
  }
  if (!StrEquals(arr4[1], "y")) {
    Print("FAIL: arr4[1]="); Print(arr4[1]); Print("\n"); return 1;
  }
  if (!StrEquals(arr4[2], "z")) {
    Print("FAIL: arr4[2]="); Print(arr4[2]); Print("\n"); return 1;
  }

  Print("OK\n");
  return 0;
}
