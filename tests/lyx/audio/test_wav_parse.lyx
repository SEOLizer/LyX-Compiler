// Minimal struct parameter test
import std.audio;

pub fn main(): int64 {
  var info: AudioInfo;
  
  // Direct field assignment works
  info.format := 10;
  info.sampleRate := 44100;
  info.channels := 2;
  
  Print("Direct works: ");
  PrintInt(info.format);
  Print("\n");
  
  // Try calling WAVParse (simpler, might work?)
  var result: int64;
  result := WAVParse("/home/andreas/PhpstormProjects/aurum/test.wav", info);
  
  Print("WAVParse result: ");
  PrintInt(result);
  Print("\n");
  
  return 0;
}
