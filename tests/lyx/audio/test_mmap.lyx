// mmap test
pub fn main(): int64 {
  var buf: int64;
  
  // Simple mmap test
  buf := mmap(0, 4096, 3, 32, -1);
  PrintStr("mmap result: ");
  PrintInt(buf);
  PrintStr("\n");
  
  if (buf != 0) {
    munmap(buf, 4096);
  }
  
  return 0;
}