// mmap test
pub fn main(): int64 {
  var buf: int64;
  
  // Simple mmap test
  buf := mmap(0, 4096, 3, 32, -1);
  Print("mmap result: ");
  PrintInt(buf);
  Print("\n");
  
  if (buf != 0) {
    munmap(buf, 4096);
  }
  
  return 0;
}