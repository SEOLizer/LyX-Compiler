// Test MP3 Parser - simpler version
import std.audio;

pub fn main(): int64 {
  var info: AudioInfo;
  var result: int64;
  
  result := MP3Parse("/home/andreas/PhpstormProjects/aurum/music_test.mp3", info);
  
  // Test after parse - set some values directly
  info.format := 999;
  info.sampleRate := 999;
  info.channels := 999;
  
  Print("After direct assignment:\n");
  Print("format: ");
  PrintInt(info.format);
  Print("\n");
  Print("sampleRate: ");
  PrintInt(info.sampleRate);
  Print("\n");
  Print("channels: ");
  PrintInt(info.channels);
  Print("\n");
  
  return 0;
}
