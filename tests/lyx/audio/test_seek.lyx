// Test lseek
import std.fs;
pub fn main(): int64 {
  var path: pchar;
  path := "/home/andreas/PhpstormProjects/aurum/test.wav";
  
  var fd: int64;
  fd := open(path, O_RDONLY, 0);
  Print("fd: ");
  PrintInt(fd);
  Print("\n");
  
  if (fd < 0) {
    Print("open failed\n");
    return 1;
  }
  
  var sz: int64;
  
  // Test 1: SEEK_END from current (beginning)
  sz := lseek(fd, 0, SEEK_END);
  Print("SEEK_END: ");
  PrintInt(sz);
  Print("\n");
  
  // Test 2: SEEK_SET
  var pos: int64;
  pos := lseek(fd, 0, SEEK_SET);
  Print("SEEK_SET: ");
  PrintInt(pos);
  Print("\n");
  
  // Test 3: SEEK_END again
  sz := lseek(fd, 0, SEEK_END);
  Print("SEEK_END 2: ");
  PrintInt(sz);
  Print("\n");
  
  close(fd);
  
  return 0;
}