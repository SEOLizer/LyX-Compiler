// Minimal test - struct field assignment in imported function
import std.audio;

// Try to set field from MP3Parse function
pub fn main(): int64 {
  var info: AudioInfo;
  
  // Direct assignment in main - works
  info.format := 10;
  info.sampleRate := 48000;
  
  Print("Direct assignment works:\n");
  Print("format: ");
  PrintInt(info.format);
  Print("\n");
  Print("sampleRate: ");
  PrintInt(info.sampleRate);
  Print("\n");
  
  return 0;
}
