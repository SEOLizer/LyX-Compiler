// Test: Check what error we're getting
import std.audio;
import std.audio.alsa;

pub fn main(): int64 {
  Print("=== ALSA Check ===\n");
  
  // Try a simple beep using aplay instead
  Print("Running aplay...\n");
  
  // Use simple system call
  import std.os;
  
  // This is a simple test - just play the file with aplay
  Print("File: /tmp/playback_test.wav\n");
  Print("To play manually, run: aplay /tmp/playback_test.wav\n");
  
  // Check file properties
  var fd: int64;
  fd := open("/tmp/playback_test.wav", 0, 0);
  
  if (fd >= 0) {
    var size: int64;
    size := lseek(fd, 0, SEEK_END);
    close(fd);
    
    Print("File size: ");
    PrintInt(size);
    Print(" bytes\n");
    
    // Calculate duration
    // 44100 Hz * 2 channels * 2 bytes = 176400 bytes/sec
    var duration: int64;
    duration := size - 44;
    duration := duration * 1000 / 176400;
    
    Print("Duration: ~");
    PrintInt(duration);
    Print(" ms\n");
  }
  
  Print("=== Done ===\n");
  return 0;
}
