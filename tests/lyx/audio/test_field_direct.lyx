// Test: just do direct field assignment in main
import std.audio;

pub fn main(): int64 {
  var info: AudioInfo;
  
  // This should trigger FieldAssign in main!
  info.format := 99;
  
  Print("format: ");
  PrintInt(info.format);
  Print("\n");
  
  return 0;
}
