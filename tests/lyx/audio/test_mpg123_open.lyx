// Test mpg123 - just open file
import std.audio;
import std.audio.mpg123;

pub fn main(): int64 {
  Print("=== mpg123 Open Test ===\n");
  
  // Step 1: Initialize
  mpg123_init();
  
  // Step 2: Create handle
  var handle: int64;
  handle := mpg123_new(0, 0);
  
  if (handle == 0) {
    Print("ERROR: new failed\n");
    mpg123_exit();
    return 1;
  }
  Print("handle=");
  PrintInt(handle);
  Print("\n");
  
  // Step 3: Open file
  Print("3. mpg123_open...\n");
  var openRes: int64;
  openRes := mpg123_open(handle, "/home/andreas/PhpstormProjects/aurum/music_test.mp3");
  Print("openRes=");
  PrintInt(openRes);
  Print("\n");
  
  if (openRes != MPG123_OK) {
    Print("ERROR: open failed\n");
    mpg123_delete(handle);
    mpg123_exit();
    return 1;
  }
  
  // Step 4: Cleanup
  Print("4. cleanup...\n");
  mpg123_close(handle);
  mpg123_delete(handle);
  mpg123_exit();
  
  Print("=== Complete ===\n");
  return 0;
}
