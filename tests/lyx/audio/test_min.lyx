// Minimal debug
import std.audio;

pub fn main(): int64 {
  var fd: int64;
  
  fd := open("test.wav", O_RDONLY, 0);
  
  PrintStr("fd=");
  PrintInt(fd);
  PrintStr("\n");
  
  if (fd >= 0) {
    close(fd);
    return 0;
  }
  
  return 1;
}