// Test: Does struct parameter work at all from imported unit?
import std.audio;

pub fn main(): int64 {
  var info: AudioInfo;
  
  // First verify we can call MP3Parse (even if it fails)
  var result: int64;
  result := MP3Parse("/home/andreas/PhpstormProjects/aurum/music_test.mp3", info);
  
  PrintStr("Result: ");
  PrintInt(result);
  PrintStr("\n");
  
  // Try setting directly AFTER the call
  info.format := 99;
  
  PrintStr("After manual set - format: ");
  PrintInt(info.format);
  PrintStr("\n");
  
  return 0;
}
