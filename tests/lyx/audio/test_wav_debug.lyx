// WAV debug test
import std.audio;

pub fn main(): int64 {
  var info: AudioInfo;
  var result: int64;
  
  // Use debug version via std.audio
  result := WAVParse("/tmp/test.wav", info);
  
  PrintStr("Result: ");
  PrintInt(result);
  PrintStr("\n");
  
  return 0;
}