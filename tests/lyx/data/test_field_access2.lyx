// Test: Print struct field offsets
import tests.lyx.data.pointmod;

fn printPoint(p: Point): int64 {
  Print("point at ");
  PrintInt(p.x);
  Print(",");
  PrintInt(p.y);
  Print("\n");
  return 0;
}

fn main(): int64 {
  var pt: Point;
  // Directly access fields before passing to function
  pt.x := 100;
  pt.y := 200;
  Print("Direct access: x=");
  PrintInt(pt.x);
  Print(" y=");
  PrintInt(pt.y);
  Print("\n");
  
  // Now test via function
  printPoint(pt);
  return 0;
}
