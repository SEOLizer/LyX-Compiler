// Debug: Was gibt SeriesNewInt64 zurück?
import data.core;

fn main(): int64 {
  PrintStr("Vor SeriesNewInt64\n");
  var s: Series := SeriesNewInt64(0, 10);
  PrintStr("Nach SeriesNewInt64\n");
  
  // Prüfe die Werte
  PrintStr("data: ");
  PrintInt(s.data);
  PrintStr("\n");
  
  PrintStr("nullBitmap: ");
  PrintInt(s.nullBitmap);
  PrintStr("\n");
  
  PrintStr("length: ");
  PrintInt(s.length);
  PrintStr("\n");
  
  PrintStr("capacity: ");
  PrintInt(s.capacity);
  PrintStr("\n");
  
  return 0;
}
