// Test: Simple field access from imported module
import tests.lyx.data.pointmod;

fn main(): int64 {
  var pt: Point := makePoint(10, 20);
  
  Print("pt.x = ");
  PrintInt(getX(pt));
  Print("\n");
  
  Print("pt.y = ");
  PrintInt(getY(pt));
  Print("\n");
  
  return 0;
}
