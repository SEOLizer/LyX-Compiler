// Test: Import module with struct-returning function
import tests.lyx.data.structmod;

fn main(): int64 {
  var p: Pair := makePair(100, 200);
  PrintInt(p.x);
  PrintStr(" ");
  PrintInt(p.y);
  PrintStr("\n");
  return 0;
}
