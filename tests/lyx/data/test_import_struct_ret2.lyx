// Test: Import module with struct-returning function - with debug
import tests.lyx.data.structmod;

fn main(): int64 {
  // First, test local creation
  var localP: Pair;
  localP.x := 1000;
  localP.y := 2000;
  Print("Local: ");
  PrintInt(localP.x);
  Print(" ");
  PrintInt(localP.y);
  Print("\n");
  
  // Now test imported function
  Print("Calling makePair...\n");
  var p: Pair := makePair(100, 200);
  Print("After call, p = ");
  PrintInt(p.x);
  Print(" ");
  PrintInt(p.y);
  Print("\n");
  return 0;
}
