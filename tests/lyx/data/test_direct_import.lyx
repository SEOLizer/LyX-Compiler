// Test: Print struct field directly
import tests.lyx.data.pointmod;

fn main(): int64 {
  var pt: Point;
  pt.x := 100;
  pt.y := 200;
  
  Print("Direct in main: ");
  PrintInt(pt.x);
  Print(" ");
  PrintInt(pt.y);
  Print("\n");
  
  return 0;
}
