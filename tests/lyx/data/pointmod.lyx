// Simple module with struct field access
pub type Point = struct {
  x: int64;
  y: int64;
};

pub fn makePoint(px: int64, py: int64): Point {
  var p: Point;
  p.x := px;
  p.y := py;
  return p;
}

pub fn getX(p: Point): int64 {
  return p.x;
}

pub fn getY(p: Point): int64 {
  return p.y;
}
