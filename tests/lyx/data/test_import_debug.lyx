// Test: Import module with debug output
import tests.lyx.data.structmod_debug;

fn main(): int64 {
  PrintStr("Calling makePair from main...\n");
  var p: Pair := makePair(100, 200);
  PrintStr("Back in main, p = ");
  PrintInt(p.x);
  PrintStr(" ");
  PrintInt(p.y);
  PrintStr("\n");
  return 0;
}
