// Test: Import module with debug output
import tests.lyx.data.structmod_debug;

fn main(): int64 {
  Print("Calling makePair from main...\n");
  var p: Pair := makePair(100, 200);
  Print("Back in main, p = ");
  PrintInt(p.x);
  Print(" ");
  PrintInt(p.y);
  Print("\n");
  return 0;
}
