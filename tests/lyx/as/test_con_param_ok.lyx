// WP-AS-20: con parameter read-only — reading is fine, return value works
// Expected exit code: 11
fn add(con a: int64, con b: int64): int64 {
    return a + b;
}

fn main(): int64 {
    exit(add(5, 6));
    return 0;
}
