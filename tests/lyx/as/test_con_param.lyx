// WP-AS-20: con parameter is read-only — write to it is rejected by sema
// This file should produce a sema error (expected compile exit code: 1)
fn f(con x: int64): int64 {
    x := 5;   // ERROR: assignment to con-parameter
    return x;
}

fn main(): int64 {
    exit(f(10));
    return 0;
}
