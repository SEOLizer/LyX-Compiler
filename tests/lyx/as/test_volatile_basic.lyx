// WP-AS-20: @volatile var compiles and works correctly
// Expected exit code: 0
fn main(): int64 {
    @volatile var counter: int64 := 0;
    counter := counter + 1;
    counter := counter + 1;
    counter := counter + 1;
    if (counter == 3) {
        exit(0);
    }
    exit(1);
    return 0;
}
