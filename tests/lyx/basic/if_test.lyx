fn main(): int64 {
  let x: int64 := 10;
  
  if (x > 5) {
    PrintStr("greater");
  }
  
  if (x > 0 && x < 20) {
    PrintStr("between");
  }
  
  if (x == 0) {
    PrintStr("zero");
  } else if (x == 10) {
    PrintStr("ten");
  } else {
    PrintStr("other");
  }
  
  return x;
}
