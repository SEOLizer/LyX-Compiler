// simple_test.lyx - Einfacher Test ohne externe Calls

fn add(a: int64, b: int64): int64 {
  return a + b;
}

fn main(): int64 {
  var x: int64 := 10;
  var y: int64 := 20;
  var z: int64 := add(x, y);
  return z - 30;  // Should return 0
}
