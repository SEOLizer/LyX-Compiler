// Test: function pointer - debug indirect call
type Callback = fn(int64, int64) -> int64;

fn add(a: int64, b: int64): int64 {
  return a + b;
}

fn indirect_call(fn_ptr: int64, a: int64, b: int64): int64 {
  return 0;  // TODO: call through fn_ptr
}

fn main(): int64 {
  var cb: Callback := add;
  // Print address
  var addr: int64 := cb as int64;
  PrintInt(addr);
  // Direct call
  var result: int64 := add(10, 20);
  PrintInt(result);
  return 0;
}
