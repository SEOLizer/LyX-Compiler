// Minimal test: function pointer call
fn add(a: int64, b: int64): int64 {
  return a + b;
}

fn main(): int64 {
  // Try direct call first
  var result: int64 := add(10, 20);
  PrintInt(result);  // Should print 30
  
  return 0;
}
