// Test: function pointer - check cast type with actual use
fn add(a: int64, b: int64): int64 {
  return a + b;
}

fn call_it(f: int64, x: int64, y: int64): int64 {
  // Use the function somehow
  return x + y;
}

fn main(): int64 {
  var addr: int64 := add as int64;
  var result: int64 := call_it(addr, 10, 20);
  PrintInt(result);
  return 0;
}
