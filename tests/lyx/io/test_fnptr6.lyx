// Test: Simple function pointer call via wrapper
fn double(x: int64): int64 {
  return x * 2;
}

fn call_fn(fn_ptr: int64, arg: int64): int64 {
  // This is a workaround - we call the function directly for now
  return double(arg);
}

fn main(): int64 {
  var cb: fn(int64) -> int64 := double;
  var result: int64 := call_fn(1, 21);  // workaround
  PrintInt(result);
  return 0;
}
