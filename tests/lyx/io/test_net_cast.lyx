// Test: the actual use case from std.net
// Simulating the std.net use case
fn main(): int64 {
  var portPtr: int64 := 80;  // Simulate memory with port 80
  
  // This is the actual use case from std.net:
  // addr.port := (peek8(portPtr) | (peek8(portPtr + 1) << 8)) as uint16;
  // But peek8 is not available, so we use direct memory access
  
  // Let's test just the cast on expression
  var low: int64 := 80;
  var high: int64 := 0;
  var combined: int64 := low | (high << 8);
  var port: int64 := combined as uint16;
  
  PrintInt(port);
  return 0;
}
