fn addVectors(a: parallel Array<int64>, b: parallel Array<int64>): parallel Array<int64> {
  return a + b;
}

fn main(): int64 {
  let x: parallel Array<int64>(4) = parallel Array<int64>(4);
  let y: parallel Array<int64>(4) = parallel Array<int64>(4);
  let z: parallel Array<int64> = addVectors(x, y);
  return 0;
}
