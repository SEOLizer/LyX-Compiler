// Simple test: just load and print an int64 field without casting first

type TTest = struct {
  a: int64;
};

fn main(): int64 {
  var s: TTest;
  s.a := 10;
  
  return s.a;  // implicit cast to int64 for return
}
