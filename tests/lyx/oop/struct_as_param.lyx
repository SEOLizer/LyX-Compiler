// Test struct as function parameter
type Point := struct {
  x: int64;
  y: int64;
};

fn getX(p: Point): int64 {
  return p.x;
}

fn main(): int64 {
  var p: Point := Point { x: 100, y: 200 };
  return getX(p);  // Should return 100
}
