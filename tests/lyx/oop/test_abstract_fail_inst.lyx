// Test: abstract - should fail when trying to instantiate abstract class

type Animal = class {
  abstract fn Speak();
};

fn main(): int64 {
  // This should cause an error: cannot instantiate abstract class
  var a: Animal := new Animal();
  return 0;
}
