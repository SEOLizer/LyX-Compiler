// Dynamisches Array waechst ueber die erste Belegung hinaus.
//
// #1153: Die Datei gab `len(arr)` als Rueckgabewert zurueck — vier `push`,
// also exit=4. Der Wert war richtig, der Runner erwartet aber 0. Statt die 4
// als `.expected-exit` festzuschreiben (das haette den Ist-Zustand zum Soll
// erklaert), prueft der Test jetzt selbst und sagt beim Scheitern, WAS nicht
// stimmte.
fn main(): int64 {
  var arr: int64[];
  push(arr, 1);
  push(arr, 2);
  push(arr, 3);
  push(arr, 4);
  if (len(arr) != 4) { return 1; }
  if (arr[0] != 1) { return 2; }
  if (arr[3] != 4) { return 3; }   // ueber die anfaengliche Belegung hinaus
  PrintStr("OK: dynarray waechst\n"c);
  return 0;
}
