// Test program for precompiled units - using .lyx source
// Note: For full .lyu support, the deserialized symbols need to be 
// integrated into the compiler's symbol tables
import tests.lyx.precompiled.myunit;

fn main(): int64 {
  var msg: pchar;
  msg := getMessage();
  PrintStr(msg);
  PrintStr("\n");
  return 0;
}