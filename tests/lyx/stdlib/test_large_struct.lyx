// Test Large Struct - wie MySQLConn (18 Felder, 144 Bytes)
import std.io;

type LargeConn = struct {
    fd: int64;
    host: pchar;
    port: int64;
    user: pchar;
    password: pchar;
    database: pchar;
    packet_num: int64;
    last_payload: int64;
    server_ver: int64;
    cap_flags: int64;
    charset: int64;
    status: int64;
    scramble: int64;
    last_error: int64;
    errmsg: pchar;
    error_num: int64;
    affected: int64;
    insert_id: int64;
    field_count: int64;
};

fn createLarge(host: pchar, pass: pchar): LargeConn {
    var conn: LargeConn;
    conn.host := host;
    conn.password := pass;
    Print("  Inside create:\n");
    Print("    conn.host = ");
    Print(conn.host);
    Print("\n");
    Print("    conn.pass = ");
    Print(conn.password);
    Print("\n");
    return conn;
}

pub fn main(): int64 {
    Print("=== Large Struct Test ===\n");
    
    var conn: LargeConn := createLarge("myhost", "mypass");
    
    Print("After return:\n");
    Print("  conn.host = ");
    Print(conn.host);
    Print("\n");
    Print("  conn.pass = ");
    Print(conn.password);
    Print("\n");
    
    Print("=== Test Complete ===\n");
    return 0;
}