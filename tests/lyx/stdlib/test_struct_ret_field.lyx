// Test struct return + field cast - wie MySQL
import std.io;

type TConn = struct {
    fd: int64;
    pass: pchar;
    other: int64;
};

fn createConn(passArg: pchar): TConn {
    var conn: TConn;
    conn.fd := 100;
    conn.pass := passArg;
    conn.other := 200;
    return conn;
}

pub fn main(): int64 {
    Print("=== Struct Return + Field Cast Test ===\n");
    
    var conn: TConn := createConn("mypassword");
    
    // Cast field to int64
    var addr: int64 := conn.pass as int64;
    Print("Result addr = ");
    PrintInt(addr);
    Print("\n");
    
    if (addr > 0) {
        var firstChar: int64 := peek8(addr);
        Print("First char = ");
        PrintInt(firstChar);
        Print(" (expected 109 = 'm')\n");
    }
    
    Print("=== Test Complete ===\n");
    return 0;
}