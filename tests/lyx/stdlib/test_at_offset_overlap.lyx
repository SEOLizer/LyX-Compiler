// WP-A3: at(offset) overlap — this MUST fail with a compile error
// Fields at offsets 0 and 2 with u32 (4 bytes) each overlap: [0,4) ∩ [2,6)

type BadStruct = struct {
    a: u32 at(0);
    b: u32 at(2);   // overlaps: a covers bytes 0-3, b starts at 2
};

pub fn main(): int64 {
    return 0;
}
