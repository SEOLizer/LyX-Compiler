// Ganz einfacher Test
import std.io;
import std.alloc;

pub fn main(): int64 {
    Print("=== Simple Test ===\n");
    
    var buf: int64 := alloc(100);
    Print("buf = ");
    PrintInt(buf);
    Print("\n");
    
    poke8(buf, 65);
    poke8(buf + 1, 66);
    
    Print("peek: ");
    PrintInt(peek8(buf));
    Print(" ");
    PrintInt(peek8(buf + 1));
    Print("\n");
    
    Print("=== Done ===\n");
    return 0;
}