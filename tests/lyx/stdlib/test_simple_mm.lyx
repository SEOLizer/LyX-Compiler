// Ganz einfacher Test
import std.io;

pub fn main(): int64 {
    PrintStr("=== Simple Test ===\n");
    
    var buf: int64 := mmap(0, 100, 3, 34, -1, 0);
    PrintStr("buf = ");
    PrintInt(buf);
    PrintStr("\n");
    
    poke8(buf, 65);
    poke8(buf + 1, 66);
    
    PrintStr("peek: ");
    PrintInt(peek8(buf));
    PrintStr(" ");
    PrintInt(peek8(buf + 1));
    PrintStr("\n");
    
    PrintStr("=== Done ===\n");
    return 0;
}