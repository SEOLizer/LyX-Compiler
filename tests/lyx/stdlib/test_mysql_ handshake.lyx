// Simple debug test for MySQL handshake

import std.net.syscalls;
import std.io;
import std.alloc;

pub fn main(): int64 {
    var fd: int64 := sys_socket(2, 1, 0);
    if (fd < 0) {
        Print("Socket error\n");
        return 1;
    }
    
    var addr: int64 := alloc(16);
    poke8(addr, 2);
    poke8(addr + 1, 0);
    poke8(addr + 2, 51);
    poke8(addr + 3, 51);
    poke8(addr + 4, 127);
    poke8(addr + 5, 0);
    poke8(addr + 6, 0);
    poke8(addr + 7, 1);
    var i: int64 := 0;
    while (i < 8) {
        poke8(addr + 8 + i, 0);
        i := i + 1;
    }
    
    var res: int64 := sys_connect(fd, addr, 16);
    if (res < 0) {
        Print("Connect error\n");
        return 1;
    }
    
    var buf: int64 := alloc(10);
    var n: int64 := sys_read(fd, buf, 10);
    if (n <= 0) {
        Print("Read error\n");
        return 1;
    }
    
    i := 0;
    while (i < n) {
        var b: int64 := peek8(buf + i);
        PrintInt(b);
        Print(" ");
        i := i + 1;
    }
    Print("\n");
    
    return 0;
}