import std.db.mysql;
import std.io;

pub fn main(): int64 {
    Print("=== MySQL Authentication Complete Test ===\n");
    
    // Connect with username 'lyx' and password 'test12345'
    var conn: MySQLConn := MySQLConnect("127.0.0.1", 3306, "lyx", "test12345", "lyx");
    
    if (conn.fd < 0) {
        Print("FAILED: Could not connect, error=");
        PrintInt(conn.error_num);
        return 1;
    }
    
    Print("SUCCESS: Connected! fd=");
    PrintInt(conn.fd);
    Print("\n");
    
    // Simple query
    var res: MySQLResult := MySQLQuery(conn, "SELECT 1");
    if (res.field_count > 0) {
        Print("Query success! field_count=");
        PrintInt(res.field_count);
        Print("\n");
    }
    
    MySQLFreeResult(res);
    MySQLClose(conn);
    
    Print("=== ALL TESTS PASSED ===\n");
    return 0;
}