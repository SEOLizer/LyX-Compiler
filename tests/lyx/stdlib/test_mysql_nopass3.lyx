import std.db.mysql;
import std.io;

pub fn main(): int64 {
    Print("MySQL Test (User: lyx, no password)\n");
    
    // Connect OHNE Passwort
    var conn: MySQLConn := MySQLConnect("127.0.0.1", 3306, "lyx", "", "lyx");
    
    if (conn.fd < 0) {
        Print("Connect failed: ");
        PrintInt(conn.error_num);
        Print("\n");
        return 1;
    }
    
    Print("Connected!\n");
    
    // Simple query
    var res: MySQLResult := MySQLQuery(conn, "SELECT 1 AS test");
    
    if (conn.error_num != 0) {
        Print("Query failed: ");
        PrintInt(conn.error_num);
        Print("\n");
    }
    else {
        Print("Query success!\n");
    }

    MySQLFreeResult(res); 
    MySQLClose(conn);
    Print("Test abgeschlossen.\n");
    return 0;
}