// Test: mmap + poke8 + write_raw
fn main(): int64 {
  let buf: int64 = mmap(0, 4096, 3, 34, -1, 0);
  poke8(buf, 0x41);
  poke8(buf + 1, 0x42);
  poke8(buf + 2, 0x43);
  poke8(buf + 3, 0x0A);
  write_raw(1, buf, 4);
  munmap(buf, 4096);
  return 0;
}
