// Test: write_raw direkt
fn main(): int64 {
  let buf: int64 = mmap(0, 4096, 3, 34, -1, 0);
  poke8(buf, 0x41);
  poke8(buf + 1, 0x0A);
  let n: int64 = write_raw(1, buf, 2);
  munmap(buf, 4096);
  return 0;
}
