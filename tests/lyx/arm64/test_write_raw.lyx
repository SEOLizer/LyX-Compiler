// Test: write_raw direkt
import std.alloc;
fn main(): int64 {
  let buf: int64 = alloc(4096);
  poke8(buf, 0x41);
  poke8(buf + 1, 0x0A);
  let n: int64 = write_raw(1, buf, 2);
  free(buf, 4096);
  return 0;
}
