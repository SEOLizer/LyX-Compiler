// Map: Lesen ueber den Schluessel.
//
// #1153: Die Datei trug den Vermerk "BUG: Map-Zugriff gibt immer 0 zurueck
// wegen fehlender x86_64 Backend Implementierung" und gab den gelesenen Wert
// als Rueckgabewert zurueck — der Runner sah exit=200 und faerbte rot.
//
// Nachgemessen: der Zugriff liefert 200, der beschriebene Defekt existiert
// nicht mehr. Der Test prueft jetzt selbst und meldet beim Scheitern, WAS
// nicht stimmte, statt den Wert nach aussen zu reichen.
fn main(): int64 {
  var scores: Map<int64, int64> := {1: 100, 2: 200};
  if (scores[2] != 200) { return 1; }
  if (scores[1] != 100) { return 2; }
  if (len(scores) != 2) { return 3; }
  PrintStr("OK: Map-Zugriff ueber den Schluessel\n"c);
  return 0;
}
