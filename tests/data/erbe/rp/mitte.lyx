unit rp.mitte;
import rp.basis;

// Zwischenstufe, ebenfalls in einer eigenen Unit: damit laeuft die Kette ueber
// ZWEI Modulgrenzen. Bei nur einer Stufe wuerde ein Fehler in der Fortsetzung
// nicht auffallen.
pub type TMitte = class extends TBasis {
  fn Create(): void { super.Create(); }
  fn Eigen(): int64 { return 100; }
}
