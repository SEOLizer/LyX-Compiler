unit m.a;
import std.io;
fn helper(x: int64, y: int64, z: int64): void {
  PrintLn("A.helper x=" + IntToStr(x) + " y=" + IntToStr(y) + " z=" + IntToStr(z));
}
pub fn CallA(): void { helper(1, 2, 3); }
