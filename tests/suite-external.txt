# Brauchen Zugangsdaten oder laufende Dienste (AWS, Cloudflare,
# DigitalOcean, Postgres, S3/MinIO) und werden deshalb nicht AUSGEFUEHRT.
#
# UEBERSETZT werden sie sehr wohl: `make test-external` prueft jeden Eintrag.
# Das braucht keine Zugangsdaten und faengt den Verfall ab, der hier lange
# unbemerkt blieb — beim ersten Lauf uebersetzten 13 von 24 nicht (#1004).
#
# Eintraege mit `# BROKEN` brechen den Lauf nicht ab; jeder hat ein Issue.
aws_demo1_s3   # BROKEN #1061 -- AWSRegionFromEnv / S3CreateBucket fehlen
aws_demo2_ec2
aws_demo3_dynamodb
aws_demo4_lambda_sqs
aws_demo5_cloudwatch
aws_demo6_secrets_iam
cf_demo1_dns
cf_demo2_workers_kv_r2
cf_demo3_d1
cf_demo4_tunnel
cf_demo5_waf_analytics
cf_integration   # BROKEN #1061 -- nutzt &x (Adress-Operator gibt es nicht)
demo_pg_copy
demo_pg_stmt
demo_s3_bulk   # BROKEN #1061 -- AWSRegionFromEnv / S3CreateBucket fehlen
demo_s3_crud   # BROKEN #1061 -- AWSRegionFromEnv / S3CreateBucket fehlen
demo_s3_list   # BROKEN #1061 -- AWSRegionFromEnv / S3CreateBucket fehlen
demo_s3_minio   # BROKEN #1061 -- nutzt &x (Adress-Operator gibt es nicht)
demo_s3_multipart   # BROKEN #1061 -- nutzt &x (Adress-Operator gibt es nicht)
demo_s3_presign   # BROKEN #1061 -- AWSRegionFromEnv / S3CreateBucket fehlen
do_demo1_webserver
do_demo2_database
do_demo3_kubernetes
s3_integration   # BROKEN #1061 -- nutzt &x (Adress-Operator gibt es nicht)
