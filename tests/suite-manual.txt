# Tests, die weder automatisch beurteilbar noch auf diesem Host lauffaehig sind.
# Sie werden von keinem Ziel ausgefuehrt und sind hier gefuehrt, damit der
# Bestand vollstaendig zugeordnet bleibt.

# Reproduktionsfall zu einem Bug, kein Pass/Fail-Test: liefert Exit 41 ohne
# Ausgabe. Der Exit-Code IST das Ergebnis und will von Hand gelesen werden.
oop_field_kernel_repro

# Haengt nach 26 PASS-Zeilen in einer Netzwerkoperation (Exit 124). Braucht
# entweder ein Zeitlimit im Test selbst oder eine Gegenstelle.
test_ipv6

# ARM64-Test: uebersetzt fuer ein anderes Ziel und laeuft auf x86 nicht
# (Exit 1 ohne Ausgabe). Gehoert in ein plattformabhaengiges Ziel.
wp03_arm64_argc_argv

# Laufzeittest fuer macOS: legt echte Sockets mit BSD-Konstanten an und ist
# deshalb nur auf macOS aussagekraeftig. Auf Linux scheiterte er zwangslaeufig
# (#1017). Die Syscall-ZUORDNUNG, die sein Kopf dokumentiert, prueft
# tests/macos_socket_syscalls_test.sh am erzeugten Mach-O — das geht auch hier.
wp06_macos_socket
# #1150: braucht einen Pfad als Programmargument ("usage: test_filereadall
# <path> [--check=value]"). suite-full kann nur Uebersetzungsoptionen
# durchreichen, keine Programmargumente — deshalb dort dauerhaft rot, obwohl
# der Test in Ordnung ist.
feature_checks/stdlib/test_filereadall
