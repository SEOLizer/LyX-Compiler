// LX-23 Test: alloc, poke8, peek8, free — LyxOS mmap-Arena.

import src.std.alloc;

fn main(): int64 {
    var buf: int64 := alloc(16);
    poke8(buf, 42);
    var v: int64 := peek8(buf);
    free(buf, 16);
    return v - 42;
}
