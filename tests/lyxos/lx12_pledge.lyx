// LX-23 Test: sys_pledge + ERR_CAPVIOL-Erkennung — nur Kompilierung prüfen.

import src.std.lyxos.caps;
import src.std.lyxos.errno;

fn main(): int64 {
    var _, err := sys_pledge(RIGHT_READ + RIGHT_WRITE, 0);
    if err == ERR_CAPVIOL {
        return 1;
    }
    return 0;
}
