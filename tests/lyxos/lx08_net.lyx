// LX-23 Test: TCP-Socket-Erstellung (sys_socket) — nur Kompilierung prüfen.

import src.std.lyxos.net;

fn main(): int64 {
    var sockfd: int64 := sys_socket(AF_INET, SOCK_STREAM, IPPROTO_TCP);
    if sockfd < 0 {
        return 1;
    }
    return 0;
}
