// LX-23 Test: sys_mutex_lock + sys_mutex_unlock — nur Kompilierung prüfen.

import src.std.alloc;
import src.std.lyxos.ipc;

fn main(): int64 {
    var mutex: int64 := alloc(8);
    poke64(mutex, MUTEX_UNLOCKED);
    sys_mutex_lock(mutex);
    sys_mutex_unlock(mutex);
    return 0;
}
