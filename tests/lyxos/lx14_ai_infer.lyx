// LX-23 Test: sys_ai_infer_sync — ERR_NOTSUP graceful — nur Kompilierung prüfen.

import src.std.lyxos.ai;
import src.std.lyxos.errno;

fn main(): int64 {
    var _, err := sys_ai_infer_sync(0, 0, 0 as pchar, 0, 0 as pchar, 0);
    if err == ERR_NOTSUP {
        return 0;
    }
    return 0;
}
