// LX-23 Test: sys_spawn + sys_wait — nur Kompilierung prüfen.

import src.std.lyxos.process;

fn main(): int64 {
    var pid: int64 := sys_spawn("/bin/true"c, 9, 0 as pchar, 0, 0);
    if pid > 0 {
        sys_wait(pid, 0, 0);
    }
    return 0;
}
