// LX-23 Test: sys_task_spawn (parallele Tasks) — nur Kompilierung prüfen.

import src.std.lyxos.task;

fn main(): int64 {
    var tid: int64 := sys_task_spawn(0, 0, TASK_DETACHED, 65536);
    if tid < 0 {
        return 1;
    }
    return 0;
}
