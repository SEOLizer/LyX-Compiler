fn main(): int64 {
  var a: array := [];
  push(a, 42);
  push(a, 100);
  var p: int64 := a[0];
  var q: int64 := a[1];
  if (p + q == 142) { PrintLn("PASS"); } else { Print("FAIL sum="); PrintLn(p + q); }
  return 0;
}
