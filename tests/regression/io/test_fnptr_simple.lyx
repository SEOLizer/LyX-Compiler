// Test: function pointer - simpler version
fn add(a: int64, b: int64): int64 {
  return a + b;
}

// Wrapper function to get function address
fn get_add_addr(): int64 {
  return add as int64;
}

fn call_fn(fn_ptr: int64, a: int64, b: int64): int64 {
  // Call through function pointer
  // Use inline assembly or workaround
  return 0;
}

fn main(): int64 {
  var addr: int64 := get_add_addr();
  PrintInt(addr);
  return 0;
}
