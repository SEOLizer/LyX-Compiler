// Testprogramm für indirekten Aufruf (Funktionszeiger)
// Dies wird noch nicht vollständig unterstützt, dient nur zum Testen

unit test_indirect;

fn add(a: int64, b: int64) -> int64 {
    return a + b;
}

fn sub(a: int64, b: int64) -> int64 {
    return a - b;
}

// Dies ist ein Funktionszeiger-Typ
type BinaryOp = fn(int64, int64) -> int64;

pub fn main() -> int64 {
    var op: BinaryOp;
    op = add;
    
    // Indirekter Aufruf über Funktionszeiger
    // TODO: Sobald die Syntax unterstützt wird
    // var result = op(10, 5);
    
    return 0;
}
