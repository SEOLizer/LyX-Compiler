// Test: exact syntax from net.md
fn foo(): int64 {
  return 42;
}

fn main(): int64 {
  var cond: bool := true;
  
  // Exact syntax from net.md: if (cond) { var x := foo(); }
  if (cond) { var x: int64 := foo(); PrintInt(x); }
  
  return 0;
}
