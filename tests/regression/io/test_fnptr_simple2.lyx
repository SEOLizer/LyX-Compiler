// Test: function pointer - check symbol types
type Callback = fn(int64, int64) -> int64;

fn add(a: int64, b: int64): int64 {
  return a + b;
}

fn main(): int64 {
  var cb1: Callback := add;
  
  // First just print the address
  var addr: int64 := cb1 as int64;
  PrintInt(addr);
  
  return 0;
}
