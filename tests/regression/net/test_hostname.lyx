// Test mmap usage
import std.net.types;

fn main(): int64 {
  var p1: int64;
  var p2: int64;
  
  Print("=== mmap Test ===\n");
  
  p1 := mmap(0, 128, PROT_RW, MAP_ANON, FD_NONE, 0);
  Print("First mmap: ");
  PrintInt(p1);
  Print("\n");
  
  p2 := mmap(0, 128, PROT_RW, MAP_ANON, FD_NONE, 0);
  Print("Second mmap: ");
  PrintInt(p2);
  Print("\n");
  
  if (p1 != 0) {
    munmap(p1, 128);
  }
  if (p2 != 0) {
    munmap(p2, 128);
  }
  
  Print("=== Test Complete ===\n");
  return 0;
}
