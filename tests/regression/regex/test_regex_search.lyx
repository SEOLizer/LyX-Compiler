// test_regex_search.lyx
// Teste RegexSearch (std.regex)
import std.regex;

fn main(): int64 {
  // Suche Position
  var pos: int64 := RegexSearch(r"world", "hello world");
  Print("Position von 'world' in 'hello world': ");
  PrintInt(pos);
  Print("\n");

  if (pos >= 0) {
    Print("OK: Pattern gefunden\n");
  } else {
    Print("FAIL: Pattern nicht gefunden\n");
  }

  // Nicht gefunden -> -1
  var pos2: int64 := RegexSearch(r"xyz", "hello world");
  Print("Position von 'xyz' in 'hello world': ");
  PrintInt(pos2);
  Print("\n");

  if (pos2 == -1) {
    Print("OK: Pattern korrekt nicht gefunden\n");
  } else {
    Print("FAIL: Pattern sollte nicht gefunden werden\n");
  }

  return 0;
}
