var globalCounter: int64 := 0;

fn increment() {
  globalCounter := globalCounter + 1;
}

fn main(): int64 {
  PrintInt(globalCounter);
  Print("\n");
  
  increment();
  increment();
  increment();
  
  PrintInt(globalCounter);
  Print("\n");
  
  return 0;
}
