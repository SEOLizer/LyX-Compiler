fn main(): int64 {
  var a: array := [];
  push(a, 42);
  push(a, 100);
  var l: int64 := len(a);
  // #1150: Ergebnis drucken statt es im Exit-Code zu tragen — der
  // Suite-Runner urteilt nach der Ausgabe.
  if (l == 2) { PrintLn("PASS len=2"); } else { Print("FAIL len="); PrintLn(l); }
  return 0;
}
