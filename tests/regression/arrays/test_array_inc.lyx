fn main(): int64 {
  var arr: array := [10, 20, 30];
  var idx: int64 := 0;
  
  Print("arr[0] before: ");
  PrintInt(arr[0]);
  Print("\n");
  
  arr[idx]++;
  
  Print("arr[0] after ++: ");
  PrintInt(arr[0]);
  Print("\n");
  
  idx := 1;
          arr[idx]--;
          
  Print("arr[1] after --: ");
          PrintInt(arr[1]);
          Print("\n");
      
          return 0;
        }
