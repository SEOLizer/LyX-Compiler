// Simple map test
fn main(): int64 {
  var m: Map<int64, int64> := {};
  m[5] := 99;
  var x: int64 := m[5];
  // #1150: Der Test endete mit `exit(x)` — also mit 99. Das war als eigenes
  // Erfolgssignal gemeint ("exit with value so we can check"), nur kennt der
  // Runner diese Konvention nicht: er urteilt nach der Ausgabe und faellt auf
  // den Exit-Code zurueck, wenn sie nichts hergibt. 99 hiess dort rot.
  // Jetzt urteilt der Test selbst — so, wie es die Regeln des Repos verlangen.
  if x == 99 { PrintLn("PASS simple_map"c); } else { PrintLn("FAIL simple_map"c); }
  return 0;
}
