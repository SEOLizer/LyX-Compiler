// Debug contains operation
fn main() {
  var m: Map<int64, int64> := {};
  m[5] := 99;
  
  var found: bool := 5 in m;
  
  // Print the bool value as int
  if (found) {
    PrintInt(1);
  } else {
    PrintInt(0);
  }
  
  exit(0);
}
