// #1150: war `extern fn printf(f: pchar, ...)` auf libc. Variadische
// Format-String-Funktionen sind Klasse 3 der FFI-Sandbox; der eingebaute
// Printf ist der Weg dahin.
fn main(): int64 {
  Printf("Hello %d\n", 42);
  return 0;
}
