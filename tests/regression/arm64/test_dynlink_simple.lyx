// Test ARM64 Dynamic Linking
// Uses libc for exit() to test PLT/GOT mechanism
extern fn _exit(code: int64);

fn main(): int64 {
  Print("Hello ARM64 Dynamic Linking!\n");
  Print("Testing PLT/GOT with _exit...\n");
  _exit(0);
  return 0;  // Never reached
}
