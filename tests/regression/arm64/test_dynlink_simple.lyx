// Test ARM64 Dynamic Linking
// #1150: rief libc-_exit ueber ein unbekanntes FFI-Symbol. Der PLT/GOT-Weg
// wird von regression/arm64/test_dynamic_link geprueft (strlen); hier bleibt
// der Abbruch mit dem eingebauten `exit`.
fn main(): int64 {
  Print("Hello ARM64 Dynamic Linking!\n");
  exit(0);
  return 0;
}
