// Lint-Test: Leerer Block (W007)
fn main(): int64 {
  var x: int64 := 1;
  if (x == 1) {
    // leerer Block - W007
  }
  // #1150: Ergebnis geprueft drucken; der Suite-Runner urteilt nach der
  // Ausgabe, ein Exit-Code != 0/42 gilt ihm als rot.
  if (x == 1) { PrintLn("PASS"); } else { PrintLn("FAIL"); }
  return 0;
}
