// Lint-Test: Shadowing (W008)
fn main(): int64 {
  var x: int64 := 1;
  if (x == 1) {
    var x: int64 := 2;    // W008: shadowed variable
    if (x == 2) { PrintLn("PASS innere x sichtbar"); } else { PrintLn("FAIL"); }
  }
  return 0;
}
