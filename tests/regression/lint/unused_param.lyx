// Lint-Test: Ungenutzter Parameter (W002)
fn Add(a: int64, b: int64): int64 {  // W002: b wird nie gelesen
  return a;
}

fn main(): int64 {
  // #1150: Ergebnis geprueft drucken; der Suite-Runner urteilt nach der
  // Ausgabe, ein Exit-Code != 0/42 gilt ihm als rot.
  if (Add(1, 2) == 1) { PrintLn("PASS"); } else { PrintLn("FAIL"); }
  return 0;
}
