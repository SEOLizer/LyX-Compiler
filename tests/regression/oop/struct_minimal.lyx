// Test struct with two fields - both fields
type Point = struct {
  x: int64;
  y: int64;
};

fn main(): int64 {
  var p: Point;
  p.x := 10;
  p.y := 20;
  let sum: int64 := p.x + p.y;
  if (sum == 30) { PrintLn("PASS"); } else { Print("FAIL sum="); PrintLn(sum); }
  return 0;
}
