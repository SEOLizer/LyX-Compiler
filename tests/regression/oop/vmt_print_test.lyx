// VMT test that prints results
type Base = class {
  virtual fn Foo(): int64 {
    return 0;
  }
};

type Derived = class extends Base {
  override fn Foo(): int64 {
    return 1;
  }
};

fn main(): int64 {
  var b: Base := new Base();
  var d: Derived := new Derived();
  
  Print("Base.Foo(): ");
  PrintInt(b.Foo());
  Print("\n");
  
  Print("Derived.Foo(): ");
  PrintInt(d.Foo());
  Print("\n");
  
  dispose b;
  dispose d;
  
  return 0;
}