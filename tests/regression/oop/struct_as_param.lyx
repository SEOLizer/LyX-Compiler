// Test struct as function parameter
type Point = struct {
  x: int64;
  y: int64;
};

fn getX(p: Point): int64 {
  return p.x;
}

fn main(): int64 {
  var p: Point;
  p.x := 100;
  p.y := 200;
  if (getX(p) == 100) { PrintLn("PASS"); } else { PrintLn("FAIL"); }
  return 0;
}
