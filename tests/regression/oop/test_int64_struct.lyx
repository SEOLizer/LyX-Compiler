// Test: int64 struct field - simpler test

type TTest = struct {
  a: int64;
  b: int64;
};

fn main(): int64 {
  var s: TTest;
  s.a := 10;
  s.b := 20;
  
  Print("s.a (after assignment) = ");
  PrintInt(s.a);
  Print("\n");
  
  Print("s.b (after assignment) = ");
  PrintInt(s.b);
  Print("\n");
  
  return 0;
}
