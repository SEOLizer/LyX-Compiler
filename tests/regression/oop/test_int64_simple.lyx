// Simple test: just load and print an int64 field without casting first

type TTest = struct {
  a: int64;
};

fn main(): int64 {
  var s: TTest;
  s.a := 10;
  
  if (s.a == 10) { PrintLn("PASS"); } else { Print("FAIL a="); PrintLn(s.a); }
  return 0;
}
