con NL: pchar := "\n";

fn main(): int64 {
  let x: int64 := 10;

  // Einfaches if/else
  if (x > 5) {
    Print("x > 5\n");
  } else {
    Print("x <= 5\n");
  }

  // If ohne else (ein Statement / Block erlaubt)
  if ((x > 0) && (x < 20)) {
    Print("0 < x < 20\n");
  }

  // Verschachtelte Ifs
  if (x == 0) {
    Print("x == 0\n");
  } else {
    if (x % 2 == 0) {
      Print("x is even\n");
    } else {
      Print("x is odd\n");
    }
  }

  return 0;
}
