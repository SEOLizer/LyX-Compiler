// Test only odd function
// #1820: odd() gab es nicht; die Funktion heisst Odd und steht in std.math.
import std.math;

fn main(): int64 {
  Print("Testing odd() only...\n");
  
  Print("Odd(42) = ");
  if (Odd(42)) { Print("true"); } else { Print("false"); }
  Print("\n");
  
  Print("Odd(37) = ");
  if (Odd(37)) { Print("true"); } else { Print("false"); }
  Print("\n");
  
  return 0;
}