fn main(): int64 {
  var a: array := [];
  push(a, 42);
  var l: int64 := len(a);
  // #1150: Ergebnis geprueft drucken; der Suite-Runner urteilt nach der
  // Ausgabe, ein Exit-Code != 0/42 gilt ihm als rot.
  if (l == 1) { PrintLn("PASS len=1"); } else { Print("FAIL len="); PrintLn(l); }
  return 0;
}
