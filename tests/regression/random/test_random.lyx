// test_random.lyx
// Teste Random und RandomSeed Builtins
fn main(): int64 {
  // Seed setzen für reproduzierbare Ergebnisse
  RandomSeed(42);

  var r1: int64 := Random();
  var r2: int64 := Random();
  var r3: int64 := Random();

  Print("r1: ");
  PrintInt(r1);
  Print("\n");

  Print("r2: ");
  PrintInt(r2);
  Print("\n");

  Print("r3: ");
  PrintInt(r3);
  Print("\n");

  // Seed erneut setzen -> gleiche Sequenz
  RandomSeed(42);
  var r1b: int64 := Random();

  Print("r1b (nach reseed): ");
  PrintInt(r1b);
  Print("\n");

  if (r1 == r1b) {
    Print("OK: reseed liefert gleiche Sequenz\n");
  } else {
    Print("FAIL: reseed liefert andere Sequenz\n");
  }

  return 0;
}
