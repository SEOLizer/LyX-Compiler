// More complex self-host test with strings and loops
fn fib(n: int64): int64 {
  if n <= 1 { return n; }
  return fib(n - 1) + fib(n - 2);
}

fn main(): int64 {
  var i: int64 := 0;
  while i < 10 {
    PrintInt(fib(i));
    PrintStr(" ");
    i := i + 1;
  }
  PrintStr("\n");
  return 0;
}